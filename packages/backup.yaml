#===============================================================================
# In configuration.yaml or packages/backup.yaml
#===============================================================================
recorder:
  purge_keep_days: 7  # Reduce database size
  include:
    domains:
      - sensor
      - switch
      - automation
    entity_globs:
      - "sensor.deyeinverter*"
      - "sensor.prepaid*"
